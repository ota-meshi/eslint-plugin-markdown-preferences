# Regular expression patterns

Use Array.isArray and Object.keys. `Array.from` is also useful.

See markdown-preferences/prefer-linked-words and optional.

[Array.isArray](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/isArray) is already linked.

MyArray.isArrayish does not match a whole word.
