{
  "options": [
    {
      "padding": "never",
      "overrides": [
        {
          "info": "warning",
          "padding": "always"
        },
        {
          "info": "^code-",
          "padding": "never"
        }
      ]
    }
  ]
}
